import { TouchableOpacity, Text, Image, Linking, View } from "react-native";

export function CardBeaches() {
  return (
    <View
      style={{
        width: 65,
        height: 65,
        borderRadius: 50,
        backgroundColor: "#FF3131",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Diego</Text>
    </View>
  );
}
