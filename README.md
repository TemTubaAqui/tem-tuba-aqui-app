## tem-tuba-aqui-app
### Requirements
- Node
- npm or yarn
### Setup
- Clone repository
- Use in project root `npm install` or `yarn install`
### Usage
- Run `npx expo start` to run on device or emulator
